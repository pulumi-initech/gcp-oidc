name: gcp-oidc-multi
description: GCP OIDC setup for multiple projects with ESC integration
runtime:
  name: nodejs
  options:
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: typescript
